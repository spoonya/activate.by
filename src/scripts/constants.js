const CLASSES = {
  active: 'active',
  hidden: 'hidden'
};

const DOM = {
  filter: document.querySelector('#filter'),
  details: [...document.querySelectorAll('#accordion > details')]
};

export { CLASSES, DOM };
